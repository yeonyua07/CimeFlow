# CimeFlow Contributing Guide

CimeFlow 프로젝트에 관심을 가져주셔서 감사합니다! 여러분의 기여는 프로젝트를 더욱 발전시키는 큰 힘이 됩니다.

## 로컬 개발 환경 설정

사전 준비 사항:
- Node.js 20.9 (LTS) 이상
- npm 10 이상

```bash
# 저장소 클론 및 의존성 설치
git clone https://github.com/yeonyua07/CimeFlow.git
cd CimeFlow
npm ci  # package-lock.json 기반 재현 가능 설치 (npm install 대신 권장)

# 환경 변수 설정
cp .env.example .env.local

# 개발 모드 실행 (별도 터미널 권장)
npm run dev:relay
npm run dev:ui
```

`http://localhost:3000`에서 개발 중인 UI를 확인할 수 있습니다.

## 풀 리퀘스트(PR) 제출 전 체크리스트

PR을 제출하기 전, 다음 명령어를 통해 코드 결함 여부를 확인해 주세요:

```bash
npm run check    # 구문 오류 및 정적 검사
npm audit        # 의존성 보안 취약점 점검
npm run build    # 빌드 성공 여부 확인
```

UI 변경 사항이 있는 경우, PR 본문에 변경 전/후 스크린샷이나 동작 설명(GIF 등)을 포함해 주시면 리뷰에 큰 도움이 됩니다.

## PR 가이드라인

- **커밋 단위**: 하나의 PR은 하나의 문제나 기능에 집중해 주세요.
- **코드 스타일**: 기존 코드 컨벤션과 컴포넌트 구조를 준수해 주세요.
- **보안**: API 키, 토큰, 또는 로컬 `.env` 파일이 포함되지 않도록 주의하세요.
- **저작권**: 프로젝트의 명시적인 허가 없이 공식 ci.me 로고나 브랜드 자산을 포함하지 마세요.
- **문서화**: 새로운 기능 추가나 환경 변수 변경 시 `README.md`를 함께 업데이트해 주세요.

## 이슈(Issue) 가이드라인

버그 리포트 작성 시 아래 내용을 포함해 주세요:

- **현상**: 발생한 버그에 대한 상세 설명
- **기대 동작**: 정상적으로 작동했을 때의 시나리오
- **재현 경로**: 버그를 다시 확인할 수 있는 단계별 과정
- **환경 정보**: 브라우저, OS 버전, Node.js 버전 등
- **로그**: 민감 정보가 제거된 서버 로그 (있을 경우)

새로운 기능 제안(Feature Request) 시에는 해당 기능이 실제 스트리밍 워크플로우에서 어떻게 활용될 수 있는지 설명해 주세요.
